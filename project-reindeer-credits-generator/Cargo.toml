[package]
name = "project-reindeer-credits-generator"
version = "0.1.0"
authors = ["Anders Gaustad <anders.gaustad@gmail.com>"]
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
walkdir = "2"
