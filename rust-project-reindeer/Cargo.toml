[package]
name = "rust-project-reindeer"
version = "0.1.0"
authors = ["Anders Gaustad <anders.gaustad@gmail.com>"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { version = "0.4.5", features = ["api-4-5"] }
strum = { version = "0.26", features = ["derive"] }
